# Server
PORT=3000

# Contract: use the address from Remix IDE after deploying ReputationRegistry to Sepolia
SEPOLIA_RPC_URL=https://rpc.sepolia.org
BACKEND_CONTRACT_ADDRESS=
PRIVATE_KEY=

# reCAPTCHA v2 (free) - https://www.google.com/recaptcha/admin
RECAPTCHA_SECRET_KEY=

# Optional: allow frontend origin (omit in dev to allow any origin and fix "failed to fetch")
CORS_ORIGIN=http://localhost:5500

# Optional: Aadhaar KYC - mock (default) or external API
# Mock: any 12-digit format passes; set MOCK_AADHAAR_OTP=000000 and send otp "000000" for stricter demo
# Real API: set both to use external KYC provider (UIDAI-authorized)
# AADHAAR_KYC_API_URL=
# AADHAAR_KYC_API_KEY=
# MOCK_AADHAAR_OTP=000000
